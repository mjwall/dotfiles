#!/bin/bash
#echo $WINDOW >~/.emacsclient-caller
#screen -r -X select 1

# -a "" starts emacs daemon and reattaches
# -t starts in terminal, since I won't be using the gui
# can also pass in -n if you want to have the shell return right awqy
exec /usr/bin/env emacsclient -a "" -t $@
